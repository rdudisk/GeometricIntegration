#ifndef DEF_ABSTRACTS
#define DEF_ABSTRACTS
/*
namespace Abstract {

class BaseSpace {
public:
	virtual void
	foo ( ) = 0;
};

class ConfigSpace {
public:
	virtual void
	foo ( ) = 0;
};

class LieAlgebra {
public:

	virtual LieAlgebra& bracket (const LieAlgebra& g) = 0;

	//static LieAlgebra
	//bracket (const LieAlgebra& g1, const LieAlgebra& g2)
	//{
	//	return g1.bracket(g2);
	//}
};

} // namespace Abstract

*/
#endif
